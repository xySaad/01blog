# 01blog

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 21.1.0.
[Learn more](./docs/angular.md)

# Standard

To be able to maintain this project and add features quickly follow those standards to avoid confusion.

## Components

- when creating components it is recommended to write each component in a folder that contains `template.html` and `component.ts`; both files are considered as the entry point of the component.
